//Escreva um método com o nome somar que recebe dois valores lidos (a e b). Esse método deve retornar o valor da soma.

public static int somar(int a, int b) {
    // Este método deve retornar o resultado da soma a + b
    int soma = a+b;
    return soma;
}
